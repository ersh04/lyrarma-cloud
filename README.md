# Lyrarma Cloud

Lyrarma Cloud is an open-source cloud platform that you can fully customize.

The app is working on Go and PostgreSQL.

## Local run

1. Download from github:

   ```sh
   
   ```

2. Start app:

   ```sh
   run.sh
   ```

3. Open [http://localhost:8080](http://localhost:8080).

Scheme PostgreSQL is created automatically at startup. The value is used for registration. `BETA_TEST_KEY` from `.env`.
